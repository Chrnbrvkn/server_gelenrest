// const Router = require('express')
// const router = new Router()
// const apartController = require('../controllers/apartController');
// const multer = require('multer');
// const upload = multer({ dest: 'uploads/' })

// router.get('/', apartController.getAparts)
// router.get('/:apartId', apartController.getOneApart)
// router.post('/', upload.none(), apartController.createApart)
// router.patch('/:apartId', apartController.updateApart)
// router.delete('/:apartId', apartController.deleteApart)

// module.exports = router